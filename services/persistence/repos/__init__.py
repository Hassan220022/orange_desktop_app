"""Repository modules for the persistence layer.

Each module exposes a focused set of functions for one entity (alarms, BDT,
blobs, etc.). The Persistence facade re-exports them under sub-facades.
"""
